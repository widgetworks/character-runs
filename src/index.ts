/**
 * Count sequential runs of characters.
 * 
    
input:
    countRuns('aaaabbbcca')

expected output:
    [
        ['a', 4],
        ['b', 3],
        ['c', 2],
        ['a', 1],
    ]
   
 */
export function countRuns(text: string): [string, number][] {
    const result: [string, number][] = [];
    
    // implement here
    
    return result;
}
